<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background saat dipilih (Active) -->
    <item android:drawable="@drawable/bg_chip_active" android:state_selected="true" />
    <!-- Background saat tidak dipilih (Inactive) -->
    <item android:drawable="@drawable/bg_chip_inactive" />
</selector>